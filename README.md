# Personal Software Engineer Website

Static professional website including:
- Home, About, Learn More, and Contact pages
- Client register/sign-in (browser localStorage)
- Auth-required contact via email client (`mailto:`)
- Photo/video update uploads shown on Home (stored in localStorage)

## Run

```bash
python3 -m http.server 3000
```

Open http://localhost:3000
